class AppConstants {
  static const appName = '家教日程管家';
  static const appVersion = '1.0.0';

  // 数据库
  static const dbName = 'tutor_schedule.db';

  // 提醒
  static const defaultReminderMinutes = 30;

  // 课时费默认值
  static const defaultHourlyRate = 200.0;

  // 重复规则
  static const repeatRules = ['不重复', '每周', '每两周', '自定义'];
}
